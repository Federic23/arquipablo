module.exports = {
    filterDetectBadWord,
};

var filterDetectBadWord = (input, next) => {
    //se valida que no tenga malas palabras de una lista negra configurable

    //devuelve la review o la review en estado rejected
};