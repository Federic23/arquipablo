module.exports = {
    filterRateSemantic,
};

var filterRateSemantic = (input, next) => {
    //se envia el comentario a un sistema externo

    //devuelve y cambia la review de pending a approved  
    //o  si es rejected la deja igual
};